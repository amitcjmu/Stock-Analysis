name: Enforce Code Policies

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  policy-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Block legacy discovery endpoint usage in app code
        run: |
          echo "🔍 Checking for legacy discovery endpoints..."
          # Exclude files that document legacy removal or contain warnings about legacy code
          if grep -r "/api/v1/discovery" backend/app/ \
            --exclude-dir=__pycache__ \
            --exclude-dir="discovery" 2>/dev/null | \
            grep -v README | \
            grep -v tests | \
            grep -v "middleware/legacy_endpoint_guard.py" | \
            grep -v "router_registry.py" | \
            grep -v "LEGACY.*DEPRECATED" | \
            grep -v "deprecated.*removed" | \
            grep -v "# Legacy" | \
            grep -v "# DO NOT"; then
            echo "❌ Legacy discovery endpoints found in application code. Use /api/v1/flows or /unified-discovery." >&2
            exit 1
          fi
          echo "✅ No legacy discovery endpoints found in application code"

      - name: Block deprecated repository base imports
        run: |
          echo "🔍 Checking for deprecated repository imports..."
          if grep -r "from app\.repositories\.base import ContextAwareRepository" backend/ 2>/dev/null | grep -v README; then
            echo "❌ Deprecated ContextAwareRepository import detected. Use app.repositories.context_aware_repository." >&2
            exit 1
          fi
          echo "✅ No deprecated repository imports found"
