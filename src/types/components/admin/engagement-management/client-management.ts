/**
 * Client Management Types
 * 
 * Types for managing client information, contacts, and organizational details
 * in engagement management.
 * 
 * Generated by CC (Claude Code) - Modularized from core-types.ts
 */

export type CompanySize = 'startup' | 'small' | 'medium' | 'large' | 'enterprise';
export type ClientStatus = 'prospect' | 'active' | 'inactive' | 'former';

// Client Management Types
export interface Client {
  id: string;
  name: string;
  description?: string;
  industry: string;
  size: CompanySize;
  status: ClientStatus;
  primaryContact: ClientContact;
  additionalContacts: ClientContact[];
  address: ClientAddress;
  website?: string;
  logoUrl?: string;
  createdAt: string;
  updatedAt: string;
  tags?: string[];
  metadata?: Record<string, any>;
}

export interface ClientContact {
  id: string;
  name: string;
  email: string;
  phone?: string;
  title: string;
  department?: string;
  isPrimary: boolean;
  isActive: boolean;
}

export interface ClientAddress {
  street1: string;
  street2?: string;
  city: string;
  state: string;
  postalCode: string;
  country: string;
}
