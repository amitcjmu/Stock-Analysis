/**
 * Artifact Management Types
 * 
 * Types for managing artifacts, documents, versioning, and permissions
 * in engagement management.
 * 
 * Generated by CC (Claude Code) - Modularized from core-types.ts
 */

import { User } from './team-management';

// Artifact Management Types
export interface Artifact {
  id: string;
  name: string;
  description?: string;
  type: 'document' | 'code' | 'design' | 'data' | 'configuration' | 'media' | 'other';
  category: string;
  url: string;
  size: number;
  mimeType: string;
  version: string;
  isLatest: boolean;
  checksum?: string;
  phaseId?: string;
  deliverableId?: string;
  milestoneId?: string;
  uploadedBy: User;
  uploadedAt: string;
  lastModified: string;
  lastModifiedBy: User;
  access: 'public' | 'team' | 'restricted' | 'confidential';
  permissions: ArtifactPermission[];
  tags: string[];
  metadata: Record<string, any>;
  versionHistory: ArtifactVersion[];
}

export interface ArtifactPermission {
  userId: string;
  user: User;
  permission: 'read' | 'write' | 'admin';
  grantedBy: User;
  grantedAt: string;
}

export interface ArtifactVersion {
  version: string;
  size: number;
  checksum?: string;
  uploadedBy: User;
  uploadedAt: string;
  comments?: string;
  url: string;
}
