/**
 * Team and User Management Types
 * 
 * Types for managing teams, team members, users, and resource allocation
 * in engagement management.
 * 
 * Generated by CC (Claude Code) - Modularized from core-types.ts
 */

export type UserStatus = 'active' | 'inactive' | 'pending' | 'blocked';

// Team Management Types
export interface Team {
  id: string;
  name: string;
  description?: string;
  teamLead: User;
  members: TeamMember[];
  skills: string[];
  capacity: number;
  utilization: number;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}

export interface TeamMember {
  id: string;
  userId: string;
  user: User;
  role: string;
  responsibilities: string[];
  allocation: number; // Percentage allocation to this engagement
  startDate: string;
  endDate?: string;
  billableRate?: number;
  skills: string[];
  isLead: boolean;
  isActive: boolean;
}

export interface User {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  phone?: string;
  title: string;
  department: string;
  manager?: User;
  skills: string[];
  certifications: string[];
  status: UserStatus;
  avatarUrl?: string;
  timezone: string;
  startDate: string;
  endDate?: string;
  billableRate?: number;
  utilization: number;
  capacity: number;
  createdAt: string;
  updatedAt: string;
}
