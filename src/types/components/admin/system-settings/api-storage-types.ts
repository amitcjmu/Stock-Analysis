/**
 * API and Storage Settings Types
 * 
 * Type definitions for API configuration, storage settings,
 * and related infrastructure configurations.
 * 
 * Generated by CC modularization
 */

import type { CorsSettings } from './security-types';

export interface ApiSettings {
  version: string;
  baseUrl: string;
  timeout: number;
  retries: number;
  rateLimit: ApiRateLimit;
  authentication: ApiAuthentication;
  documentation: ApiDocumentation;
  cors: CorsSettings;
  compression: boolean;
  caching: ApiCaching;
}

export interface ApiRateLimit {
  requestsPerMinute: number;
  requestsPerHour: number;
  burstLimit: number;
}

export interface ApiAuthentication {
  methods: string[];
  tokenExpiry: number;
  refreshTokenExpiry: number;
  allowApiKeys: boolean;
}

export interface ApiDocumentation {
  enabled: boolean;
  path: string;
  version: string;
  title: string;
  description: string;
}

export interface ApiCaching {
  enabled: boolean;
  defaultTtl: number;
  maxTtl: number;
  strategy: 'memory' | 'redis' | 'database';
}

export interface StorageSettings {
  provider: 'local' | 's3' | 'gcs' | 'azure';
  localPath?: string;
  s3Settings?: S3Settings;
  gcsSettings?: GcsSettings;
  azureSettings?: AzureSettings;
  maxFileSize: number;
  allowedTypes: string[];
  encryption: boolean;
  compression: boolean;
  cdn: CdnSettings;
}

export interface S3Settings {
  bucket: string;
  region: string;
  accessKeyId: string;
  secretAccessKey: string;
  endpoint?: string;
}

export interface GcsSettings {
  bucket: string;
  projectId: string;
  keyFile: string;
}

export interface AzureSettings {
  container: string;
  accountName: string;
  accountKey: string;
}

export interface CdnSettings {
  enabled: boolean;
  url: string;
  provider: string;
  configuration?: Record<string, string | number | boolean | null>;
}