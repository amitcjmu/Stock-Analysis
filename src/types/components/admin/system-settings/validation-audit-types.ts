/**
 * Validation and Audit Types
 * 
 * Type definitions for settings validation, audit logging,
 * field definitions, and supporting types.
 * 
 * Generated by CC modularization
 */

export interface SettingsSection {
  key: string;
  title: string;
  description?: string;
  icon?: string;
  order: number;
  permissions?: string[];
  fields: SettingsField[];
}

export interface SettingsField {
  key: string;
  label: string;
  type: SettingsFieldType;
  description?: string;
  placeholder?: string;
  required?: boolean;
  validation?: SettingsValidationRule[];
  options?: FieldOption[];
  dependencies?: FieldDependency[];
  group?: string;
  order: number;
}

export interface SettingsValidationConfig {
  validateOnChange?: boolean;
  validateOnBlur?: boolean;
  showErrors?: boolean;
  stopOnFirstError?: boolean;
  customValidators?: Record<string, (value: unknown) => boolean | string>;
}

export interface SettingsValidationRule {
  type: 'required' | 'email' | 'url' | 'min' | 'max' | 'pattern' | 'custom';
  value?: unknown;
  message: string;
  validator?: (value: unknown) => boolean | Promise<boolean>;
}

export interface SettingsAuditLog {
  id: string;
  userId: string;
  userName: string;
  action: SettingsAction;
  section: string;
  field?: string;
  oldValue?: unknown;
  newValue?: unknown;
  timestamp: string;
  ipAddress?: string;
  userAgent?: string;
  metadata?: Record<string, unknown>;
}

export interface FieldOption {
  label: string;
  value: unknown;
  disabled?: boolean;
  description?: string;
}

export interface FieldDependency {
  field: string;
  value: unknown;
  operation: 'equals' | 'not_equals' | 'in' | 'not_in';
}

// Enum and union types for validation and audit
export type SettingsFieldType = 
  | 'text' 
  | 'textarea' 
  | 'email' 
  | 'url' 
  | 'password' 
  | 'number' 
  | 'boolean' 
  | 'select' 
  | 'multiselect' 
  | 'date' 
  | 'time' 
  | 'datetime' 
  | 'file' 
  | 'color' 
  | 'json';

export type SettingsAction = 
  | 'create' 
  | 'read' 
  | 'update' 
  | 'delete' 
  | 'export' 
  | 'import' 
  | 'reset';