/**
 * Branding, Maintenance, and Backup Types
 * 
 * Type definitions for branding settings, maintenance mode,
 * and backup configurations.
 * 
 * Generated by CC modularization
 */

export interface BrandingSettings {
  logo?: string;
  favicon?: string;
  primaryColor: string;
  secondaryColor: string;
  fontFamily: string;
  customCss?: string;
  footerText?: string;
  supportEmail: string;
  supportUrl?: string;
}

export interface MaintenanceSettings {
  enabled: boolean;
  message: string;
  startTime?: string;
  endTime?: string;
  allowedIps: string[];
  showCountdown: boolean;
  redirectUrl?: string;
}

export interface BackupSettings {
  enabled: boolean;
  frequency: BackupFrequency;
  retention: number;
  storage: BackupStorage;
  encryption: boolean;
  compression: boolean;
  includeFiles: boolean;
  includeDatabases: boolean;
  notifications: BackupNotifications;
}

export interface BackupNotifications {
  onSuccess: boolean;
  onFailure: boolean;
  recipients: string[];
}

// Backup-related enum types
export type BackupFrequency = 
  | 'hourly' 
  | 'daily' 
  | 'weekly' 
  | 'monthly';

export type BackupStorage = 
  | 'local' 
  | 's3' 
  | 'gcs' 
  | 'azure' 
  | 'ftp';