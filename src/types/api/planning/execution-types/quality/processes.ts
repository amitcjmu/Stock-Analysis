/**
 * Quality Processes Types
 *
 * Type definitions for quality processes, activities, inputs, outputs,
 * controls, roles, resources, and process improvement.
 *
 * Generated by CC - Claude Code
 */

export interface QualityProcess {
  id: string;
  name: string;
  description: string;
  purpose: string;
  scope: string[];
  owner: string;
  inputs: QualityProcessInput[];
  activities: QualityProcessActivity[];
  outputs: QualityProcessOutput[];
  controls: QualityProcessControl[];
  metrics: QualityProcessMetric[];
  roles: QualityProcessRole[];
  resources: QualityProcessResource[];
  improvement: ProcessImprovement;
}

export interface QualityProcessInput {
  input: string;
  description: string;
  source: string;
  format: string;
  quality_criteria: string[];
  validation: string;
}

export interface QualityProcessActivity {
  activity: string;
  description: string;
  sequence: number;
  type: 'manual' | 'automated' | 'hybrid';
  inputs: string[];
  outputs: string[];
  roles: string[];
  tools: string[];
  controls: string[];
  duration: string;
}

export interface QualityProcessOutput {
  output: string;
  description: string;
  format: string;
  quality_criteria: string[];
  recipients: string[];
  retention: string;
}

export interface QualityProcessControl {
  control: string;
  type: 'preventive' | 'detective' | 'corrective';
  description: string;
  trigger: string;
  response: string;
  monitoring: string;
  effectiveness: string;
}

export interface QualityProcessMetric {
  metric: string;
  description: string;
  purpose: string;
  calculation: string;
  target: number;
  threshold: number;
  frequency: string;
  owner: string;
}

export interface QualityProcessRole {
  role: string;
  responsibilities: string[];
  authority: string[];
  competencies: string[];
  training: string[];
  certification: string[];
}

export interface QualityProcessResource {
  resource: string;
  type: 'human' | 'technology' | 'infrastructure' | 'financial';
  allocation: string;
  utilization: string;
  optimization: string;
}

export interface ProcessImprovement {
  approach: string;
  triggers: string[];
  methods: string[];
  evaluation: string;
  implementation: string;
  monitoring: string;
}
