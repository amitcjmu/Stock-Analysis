/**
 * Quality Assessment Types
 *
 * Type definitions for quality assessments, evaluation scope, methodology,
 * execution, findings, recommendations, and action plans.
 *
 * Generated by CC - Claude Code
 */

export interface QualityAssessment {
  scope: AssessmentScope;
  methodology: AssessmentMethodology;
  execution: AssessmentExecution;
  findings: AssessmentFinding[];
  recommendations: AssessmentRecommendation[];
  action_plan: AssessmentActionPlan;
}

export interface AssessmentScope {
  areas: string[];
  processes: string[];
  deliverables: string[];
  timeframe: string;
  exclusions: string[];
}

export interface AssessmentMethodology {
  approach: string;
  methods: string[];
  criteria: string[];
  evidence: string[];
  validation: string;
}

export interface AssessmentExecution {
  team: AssessmentTeam;
  schedule: AssessmentSchedule;
  resources: string[];
  tools: string[];
  quality: string;
}

export interface AssessmentTeam {
  lead: string;
  members: TeamMember[];
  advisors: string[];
  independence: string;
  competencies: string[];
}

export interface TeamMember {
  name: string;
  role: string;
  expertise: string[];
  contribution: string;
}

export interface AssessmentSchedule {
  phases: SchedulePhase[];
  milestones: string[];
  dependencies: string[];
  risks: string[];
}

export interface SchedulePhase {
  phase: string;
  activities: string[];
  timeline: string;
  resources: string[];
  deliverables: string[];
}

export interface AssessmentFinding {
  id: string;
  category: string;
  type: 'strength' | 'weakness' | 'opportunity' | 'threat';
  severity: 'low' | 'medium' | 'high' | 'critical';
  description: string;
  evidence: string[];
  impact: string;
  root_cause: string;
}

export interface AssessmentRecommendation {
  id: string;
  priority: 'low' | 'medium' | 'high' | 'critical';
  category: string;
  description: string;
  rationale: string;
  benefits: string[];
  risks: string[];
  effort: string;
  timeline: string;
  dependencies: string[];
}

export interface AssessmentActionPlan {
  objectives: string[];
  initiatives: PlanInitiative[];
  timeline: string;
  resources: string[];
  governance: string;
  monitoring: string;
}

export interface PlanInitiative {
  initiative: string;
  description: string;
  objectives: string[];
  approach: string;
  timeline: string;
  resources: string[];
  owner: string;
  success: string[];
}
