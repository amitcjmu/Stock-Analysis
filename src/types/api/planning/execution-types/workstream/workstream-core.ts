/**
 * Workstream Core Types
 * 
 * Core workstream definitions including the main Workstream interface,
 * scope definitions, and interface specifications.
 * 
 * Generated by CC - Claude Code
 */

export interface Workstream {
  id: string;
  name: string;
  description: string;
  type: 'technical' | 'business' | 'data' | 'infrastructure' | 'integration' | 'testing';
  priority: 'low' | 'medium' | 'high' | 'critical';
  complexity: 'low' | 'medium' | 'high' | 'very_high';
  scope: WorkstreamScope;
  objectives: WorkstreamObjective[];
  deliverables: string[];
  activities: string[]; // References to activity IDs
  resources: string[]; // References to resource IDs
  timeline: string; // Reference to timeline ID
  dependencies: string[]; // References to dependency IDs
  risks: string[]; // References to risk IDs
  governance: string; // Reference to governance ID
  quality: string; // Reference to quality ID
  success: string; // Reference to success ID
}

export interface WorkstreamScope {
  boundaries: string[];
  inclusions: string[];
  exclusions: string[];
  interfaces: WorkstreamInterface[];
  dependencies: string[];
}

export interface WorkstreamInterface {
  id: string;
  name: string;
  type: 'data' | 'service' | 'api' | 'ui' | 'process';
  description: string;
  direction: 'inbound' | 'outbound' | 'bidirectional';
  protocol: string;
  format: string;
  frequency: string;
  owner: string;
  dependencies: string[];
}

export interface WorkstreamObjective {
  objective: string;
  description: string;
  measurable: boolean;
  metrics: ObjectiveMetric[];
  target: any;
  timeline: string;
}

export interface ObjectiveMetric {
  metric: string;
  type: 'quantitative' | 'qualitative';
  unit: string;
  target: number;
  threshold: number;
  measurement: string;
  frequency: string;
}