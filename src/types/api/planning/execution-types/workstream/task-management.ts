/**
 * Workstream Task Management Types
 *
 * Types for managing workstream activities, including status tracking,
 * quality control, risk management, and issue resolution.
 *
 * Generated by CC - Claude Code
 */

export interface WorkstreamActivity {
  id: string;
  name: string;
  description: string;
  type: string;
  effort: number;
  duration: number;
  resources: string[];
  dependencies: string[];
  deliverables: string[];
  status: ActivityStatus;
  quality: ActivityQuality;
  risks: ActivityRisk[];
}

export interface ActivityStatus {
  current: 'not_started' | 'in_progress' | 'on_hold' | 'completed' | 'cancelled';
  progress: number;
  health: 'green' | 'amber' | 'red';
  issues: ActivityIssue[];
  blockers: ActivityBlocker[];
}

export interface ActivityQuality {
  standards: string[];
  criteria: ActivityCriteria[];
  reviews: ActivityReview[];
  testing: ActivityTesting;
  approval: ActivityApproval;
}

export interface ActivityRisk {
  id: string;
  description: string;
  probability: number;
  impact: number;
  category: string;
  mitigation: string;
  contingency: string;
  owner: string;
  status: 'open' | 'mitigated' | 'closed';
}

export interface ActivityIssue {
  id: string;
  description: string;
  severity: 'low' | 'medium' | 'high' | 'critical';
  impact: string;
  resolution: string;
  owner: string;
  status: 'open' | 'in_progress' | 'resolved' | 'closed';
  created: string;
  updated: string;
}

export interface ActivityBlocker {
  id: string;
  description: string;
  type: 'resource' | 'dependency' | 'approval' | 'technical' | 'external';
  impact: string;
  resolution: string;
  owner: string;
  escalation: string;
  created: string;
  target: string;
}

export interface ActivityCriteria {
  criteria: string;
  description: string;
  measurable: boolean;
  threshold: number;
  verification: string;
}

export interface ActivityReview {
  type: 'peer' | 'technical' | 'business' | 'quality';
  reviewer: string;
  schedule: string;
  criteria: string[];
  outcome: 'pass' | 'conditional' | 'fail';
  feedback: string;
}

export interface ActivityTesting {
  strategy: string;
  types: string[];
  coverage: number;
  automation: number;
  schedule: string;
  environment: string;
}

export interface ActivityApproval {
  required: boolean;
  approvers: string[];
  criteria: string[];
  process: string;
  timeline: string;
}
