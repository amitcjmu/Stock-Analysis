/**
 * Workstream Objective Types
 * 
 * Type definitions for workstream objectives and metrics.
 * 
 * Generated by CC - Claude Code
 */

import type { ConfigurationValue } from '../../../../shared/config-types';

export interface WorkstreamObjective {
  objective: string;
  description: string;
  measurable: boolean;
  metrics: ObjectiveMetric[];
  target: ConfigurationValue;
  timeline: string;
}

export interface ObjectiveMetric {
  metric: string;
  type: 'quantitative' | 'qualitative';
  unit: string;
  target: number;
  threshold: number;
  measurement: string;
  frequency: string;
}