/**
 * Workstream Resource Management Types
 *
 * Types for managing workstream resources including allocation,
 * performance tracking, and constraint management.
 *
 * Generated by CC - Claude Code
 */

export interface WorkstreamResource {
  resourceId: string;
  role: string;
  allocation: number;
  timeline: string;
  skills: string[];
  cost: number;
  performance: ResourcePerformance;
  constraints: ResourceConstraint[];
}

export interface ResourcePerformance {
  utilization: number;
  productivity: number;
  quality: number;
  satisfaction: number;
  feedback: string[];
}

export interface ResourceConstraint {
  type: 'availability' | 'skill' | 'location' | 'cost' | 'security';
  description: string;
  impact: string;
  mitigation: string;
  flexibility: 'none' | 'low' | 'medium' | 'high';
}

export interface PhaseResource {
  resourceId: string;
  role: string;
  allocation: number;
  period: string;
  criticality: 'low' | 'medium' | 'high' | 'critical';
}

export interface ScheduleResourceAllocation {
  resourceId: string;
  allocation: number;
  startDate: string;
  endDate: string;
  availability: number;
  conflicts: string[];
}

export interface ResourceMetrics {
  allocation: number;
  utilization: number;
  availability: number;
  cost: number;
  trends: ResourceMetricTrend[];
}

export interface ResourceMetricTrend {
  period: string;
  value: number;
  direction: 'improving' | 'stable' | 'declining';
  forecast: number;
}

export interface ResourceForecast {
  allocation: number;
  utilization: number;
  gaps: string[];
  conflicts: string[];
}
