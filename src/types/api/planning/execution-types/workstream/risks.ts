/**
 * Workstream Risk Types
 *
 * Type definitions for workstream risk management.
 *
 * Generated by CC - Claude Code
 */

export interface WorkstreamRisk {
  riskId: string;
  description: string;
  category: 'technical' | 'resource' | 'schedule' | 'quality' | 'external';
  probability: number;
  impact: number;
  riskScore: number;
  mitigation: string;
  contingency: string;
  owner: string;
  status: 'open' | 'mitigated' | 'closed';
  monitoring: RiskMonitoring;
}

export interface RiskMonitoring {
  indicators: string[];
  frequency: string;
  thresholds: RiskThreshold[];
  escalation: RiskEscalation;
  reporting: string;
}

export interface RiskThreshold {
  indicator: string;
  threshold: number;
  action: string;
  escalation: string;
}

export interface RiskEscalation {
  triggers: string[];
  levels: EscalationLevel[];
  timeline: string;
  process: string;
}

export interface EscalationLevel {
  level: number;
  role: string;
  authority: string;
  timeline: string;
  escalation: string;
}
