/**
 * Communication Templates Types
 *
 * Type definitions for communication templates, content management,
 * customization, and governance within communication planning.
 *
 * Generated by CC - Claude Code
 */

export interface CommunicationTemplate {
  template: string;
  type: 'message' | 'email' | 'presentation' | 'document' | 'campaign';
  purpose: string;
  audience: string[];
  content: TemplateContent;
  customization: TemplateCustomization;
  governance: TemplateGovernance;
}

export interface TemplateContent {
  structure: string[];
  elements: string[];
  variables: string[];
  formatting: string[];
  branding: string[];
}

export interface TemplateCustomization {
  parameters: string[];
  rules: string[];
  validation: string;
  preview: string;
  approval: string;
}

export interface TemplateGovernance {
  ownership: string;
  approval: string;
  versioning: string;
  access: string[];
  retirement: string;
}
