/**
 * Communication Channels Types
 *
 * Type definitions for communication channels, capabilities,
 * and management within communication planning.
 *
 * Generated by CC - Claude Code
 */

export interface CommunicationChannel {
  channel: string;
  type: 'formal' | 'informal' | 'digital' | 'face_to_face' | 'hybrid';
  purpose: string;
  audience: string[];
  frequency: string;
  ownership: string;
  characteristics: ChannelCharacteristics;
  capabilities: ChannelCapabilities;
  management: ChannelManagement;
}

export interface ChannelCharacteristics {
  reach: 'individual' | 'team' | 'department' | 'organization' | 'external';
  richness: 'low' | 'medium' | 'high';
  interactivity: 'one_way' | 'two_way' | 'multi_way';
  permanence: 'temporary' | 'archived' | 'permanent';
  formality: 'informal' | 'semi_formal' | 'formal';
}

export interface ChannelCapabilities {
  content_types: string[];
  multimedia: boolean;
  real_time: boolean;
  asynchronous: boolean;
  collaboration: boolean;
  mobile: boolean;
  accessibility: string[];
}

export interface ChannelManagement {
  administration: string;
  moderation: string;
  content_approval: string;
  access_control: string;
  monitoring: string;
  escalation: string;
}
