/**
 * Communication Scheduling & Protocols Types
 * 
 * Type definitions for communication scheduling, timing, protocols,
 * and procedures within communication planning.
 * 
 * Generated by CC - Claude Code
 */

export interface CommunicationSchedule {
  event: string;
  type: 'regular' | 'milestone' | 'ad_hoc' | 'crisis' | 'campaign';
  frequency: string;
  audience: string[];
  channel: string;
  owner: string;
  content: string[];
  timing: ScheduleTiming;
  dependencies: ScheduleDependency[];
  contingency: ScheduleContingency;
}

export interface ScheduleTiming {
  start_date: string;
  end_date: string;
  time_slots: string[];
  time_zones: string[];
  blackout_periods: string[];
}

export interface ScheduleDependency {
  dependency: string;
  type: 'prerequisite' | 'enabler' | 'constraint';
  description: string;
  impact: string;
  mitigation: string;
}

export interface ScheduleContingency {
  scenarios: string[];
  alternatives: string[];
  escalation: string;
  decision_points: string[];
}

export interface CommunicationProtocol {
  protocol: string;
  purpose: string;
  scope: string[];
  rules: ProtocolRule[];
  procedures: ProtocolProcedure[];
  escalation: ProtocolEscalation;
  governance: ProtocolGovernance;
  compliance: ProtocolCompliance;
}

export interface ProtocolRule {
  rule: string;
  description: string;
  applicability: string[];
  conditions: string[];
  exceptions: ProtocolException[];
  enforcement: string;
}

export interface ProtocolException {
  exception: string;
  conditions: string[];
  approval: string;
  documentation: string;
  review: string;
}

export interface ProtocolProcedure {
  procedure: string;
  description: string;
  steps: ProcedureStep[];
  roles: string[];
  tools: string[];
  templates: string[];
}

export interface ProcedureStep {
  step: string;
  description: string;
  inputs: string[];
  activities: string[];
  outputs: string[];
  roles: string[];
  tools: string[];
}

export interface ProtocolEscalation {
  triggers: string[];
  levels: string[];
  process: string;
  timeline: string;
  authority: string;
}

export interface ProtocolGovernance {
  ownership: string;
  review: string;
  approval: string;
  updates: string;
  training: string;
}

export interface ProtocolCompliance {
  monitoring: string;
  assessment: string;
  reporting: string;
  violations: string;
  remediation: string;
}
