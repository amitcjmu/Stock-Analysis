/**
 * Stakeholder Management Types
 *
 * Type definitions for stakeholder management, engagement, profiles,
 * and relationships within communication planning.
 *
 * Generated by CC - Claude Code
 */

export interface CommunicationStakeholder {
  stakeholder: string;
  role: string;
  type: 'internal' | 'external' | 'hybrid';
  interest: 'low' | 'medium' | 'high' | 'critical';
  influence: 'low' | 'medium' | 'high' | 'critical';
  attitude: 'champion' | 'supporter' | 'neutral' | 'skeptic' | 'opponent';
  preferences: StakeholderPreference[];
  requirements: CommunicationRequirement[];
  engagement: StakeholderEngagement;
  profile: StakeholderProfile;
}

export interface StakeholderPreference {
  category: 'channel' | 'frequency' | 'format' | 'timing' | 'content';
  preference: string;
  priority: 'must_have' | 'should_have' | 'could_have';
  rationale: string;
  constraints: string[];
}

export interface CommunicationRequirement {
  requirement: string;
  type: 'information' | 'consultation' | 'collaboration' | 'decision';
  frequency: string;
  format: string[];
  timing: string;
  urgency: 'low' | 'medium' | 'high' | 'urgent';
}

export interface StakeholderEngagement {
  strategy: string;
  level: 'inform' | 'consult' | 'involve' | 'collaborate' | 'empower';
  frequency: string;
  methods: EngagementMethod[];
  feedback: EngagementFeedback;
  measurement: EngagementMeasurement;
}

export interface EngagementMethod {
  method: string;
  description: string;
  purpose: string;
  frequency: string;
  format: string;
  participants: string[];
}

export interface EngagementFeedback {
  mechanisms: string[];
  frequency: string;
  processing: string;
  response_time: string;
  escalation: string;
}

export interface EngagementMeasurement {
  metrics: string[];
  methods: string[];
  frequency: string;
  reporting: string;
  improvement: string;
}

export interface StakeholderProfile {
  demographics: StakeholderDemographics;
  psychographics: StakeholderPsychographics;
  communication_style: CommunicationStyle;
  decision_making: DecisionMaking;
  relationships: StakeholderRelationship[];
}

export interface StakeholderDemographics {
  role_level: string;
  department: string;
  location: string;
  tenure: string;
  experience: string;
}

export interface StakeholderPsychographics {
  motivations: string[];
  concerns: string[];
  values: string[];
  working_style: string;
  change_readiness: string;
}

export interface CommunicationStyle {
  preference: 'visual' | 'auditory' | 'kinesthetic' | 'reading';
  detail_level: 'high' | 'medium' | 'low';
  formality: 'formal' | 'informal' | 'mixed';
  interaction: 'one_way' | 'two_way' | 'collaborative';
}

export interface DecisionMaking {
  style: 'analytical' | 'directive' | 'conceptual' | 'behavioral';
  criteria: string[];
  timeline: string;
  influences: string[];
  authority: string;
}

export interface StakeholderRelationship {
  stakeholder: string;
  relationship: 'reports_to' | 'manages' | 'collaborates' | 'influences' | 'dependent';
  strength: 'weak' | 'medium' | 'strong';
  quality: 'poor' | 'fair' | 'good' | 'excellent';
  history: string;
}
