/**
 * WBS Validation Types
 * 
 * Type definitions for work breakdown structure validation processes.
 * 
 * Generated by CC - Claude Code
 */

export interface WBSValidation {
  framework: ValidationFramework;
  rules: ValidationRule[];
  processes: ValidationProcess[];
  automation: ValidationAutomation;
  reporting: ValidationReporting;
  improvement: ValidationImprovement;
}

export interface ValidationFramework {
  model: string;
  principles: string[];
  standards: string[];
  coverage: string[];
  frequency: string;
}

export interface ValidationRule {
  rule: string;
  category: 'completeness' | 'consistency' | 'correctness' | 'compliance';
  description: string;
  logic: string;
  severity: 'info' | 'warning' | 'error' | 'critical';
  remediation: string;
}

export interface ValidationProcess {
  process: string;
  steps: ProcessStep[];
  roles: string[];
  tools: string[];
  outputs: string[];
}

export interface ProcessStep {
  step: string;
  inputs: string[];
  activities: string[];
  outputs: string[];
  validation: string;
}

export interface ValidationAutomation {
  coverage: number;
  tools: string[];
  triggers: string[];
  scheduling: string;
  escalation: string;
}

export interface ValidationReporting {
  formats: string[];
  frequency: string;
  distribution: string[];
  escalation: string;
  retention: string;
}

export interface ValidationImprovement {
  feedback: string;
  analysis: string;
  enhancement: string;
  implementation: string;
  monitoring: string;
}