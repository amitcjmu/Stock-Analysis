/**
 * WBS Integration Types
 *
 * Type definitions for work breakdown structure integration with other systems.
 *
 * Generated by CC - Claude Code
 */

export interface WBSIntegration {
  scope: IntegrationScope;
  methods: IntegrationMethod[];
  interfaces: IntegrationInterface[];
  synchronization: IntegrationSynchronization;
  validation: IntegrationValidation;
  governance: IntegrationGovernance;
}

export interface IntegrationScope {
  systems: string[];
  processes: string[];
  data: string[];
  stakeholders: string[];
  boundaries: string[];
}

export interface IntegrationMethod {
  method: string;
  approach: string;
  tools: string[];
  protocols: string[];
  standards: string[];
}

export interface IntegrationInterface {
  interface: string;
  type: 'API' | 'file' | 'database' | 'message' | 'service';
  protocol: string;
  format: string;
  frequency: string;
  security: string;
}

export interface IntegrationSynchronization {
  strategy: string;
  frequency: string;
  triggers: string[];
  validation: string;
  conflict_resolution: string;
}

export interface IntegrationValidation {
  testing: string;
  monitoring: string;
  alerting: string;
  reconciliation: string;
  reporting: string;
}

export interface IntegrationGovernance {
  ownership: string;
  standards: string[];
  approval: string;
  change_control: string;
  compliance: string;
}
