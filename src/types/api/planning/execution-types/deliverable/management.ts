/**
 * Deliverable Management Types
 * 
 * Types for ownership, stakeholders, resources, and artifact management.
 * 
 * Generated by CC - Claude Code
 */

export interface DeliverableOwnership {
  owner: string;
  sponsor: string;
  stakeholders: DeliverableStakeholder[];
  approvers: string[];
  reviewers: string[];
  RACI: RACIMatrix;
}

export interface DeliverableStakeholder {
  stakeholder: string;
  role: string;
  interest: 'low' | 'medium' | 'high';
  influence: 'low' | 'medium' | 'high';
  engagement: StakeholderEngagement;
  communication: StakeholderCommunication;
}

export interface StakeholderEngagement {
  strategy: string;
  frequency: string;
  methods: string[];
  feedback: string[];
}

export interface StakeholderCommunication {
  channels: string[];
  frequency: string;
  content: string[];
  feedback: string;
}

export interface RACIMatrix {
  responsible: string[];
  accountable: string[];
  consulted: string[];
  informed: string[];
}

export interface DeliverableResource {
  resourceId: string;
  type: 'human' | 'technology' | 'infrastructure' | 'financial';
  role: string;
  effort: number;
  timeline: string;
  skills: string[];
  cost: number;
  availability: ResourceAvailability;
  performance: ResourcePerformanceMetrics;
}

export interface ResourceAvailability {
  total: number;
  allocated: number;
  available: number;
  conflicts: string[];
  constraints: string[];
}

export interface ResourcePerformanceMetrics {
  productivity: number;
  quality: number;
  utilization: number;
  satisfaction: number;
  trends: PerformanceTrend[];
}

export interface PerformanceTrend {
  period: string;
  metric: string;
  value: number;
  direction: 'improving' | 'stable' | 'declining';
}

export interface DeliverableArtifact {
  id: string;
  name: string;
  type: 'document' | 'code' | 'configuration' | 'test' | 'deployment';
  description: string;
  location: string;
  version: string;
  status: 'draft' | 'review' | 'approved' | 'final';
  owner: string;
  access: string[];
  metadata: ArtifactMetadata;
}

export interface ArtifactMetadata {
  created: string;
  modified: string;
  size: number;
  format: string;
  checksum: string;
  dependencies: string[];
  tags: string[];
}