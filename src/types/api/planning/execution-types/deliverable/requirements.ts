/**
 * Deliverable Requirements Types
 * 
 * Types for requirement management, traceability, verification, and validation.
 * 
 * Generated by CC - Claude Code
 */

export interface DeliverableRequirement {
  id: string;
  type: 'functional' | 'non_functional' | 'business' | 'technical' | 'regulatory';
  category: string;
  description: string;
  priority: 'low' | 'medium' | 'high' | 'critical';
  source: string;
  rationale: string;
  acceptance: string;
  traceability: RequirementTraceability;
  verification: RequirementVerification;
  validation: RequirementValidation;
  status: RequirementStatus;
}

export interface RequirementTraceability {
  sourceId: string;
  derivedFrom: string[];
  derivesTo: string[];
  satisfiedBy: string[];
  verifiedBy: string[];
  validatedBy: string[];
}

export interface RequirementVerification {
  method: 'inspection' | 'analysis' | 'demonstration' | 'testing';
  criteria: string[];
  procedures: string[];
  tools: string[];
  environment: string;
  evidence: string[];
}

export interface RequirementValidation {
  method: string;
  stakeholders: string[];
  criteria: string[];
  scenarios: string[];
  acceptance: string;
  evidence: string[];
}

export interface RequirementStatus {
  current: 'draft' | 'review' | 'approved' | 'implemented' | 'verified' | 'validated';
  progress: number;
  issues: RequirementIssue[];
  changes: RequirementChange[];
  approval: RequirementApprovalStatus;
}

export interface RequirementIssue {
  id: string;
  type: 'ambiguity' | 'conflict' | 'incompleteness' | 'infeasibility';
  description: string;
  impact: string;
  resolution: string;
  status: 'open' | 'resolved' | 'closed';
}

export interface RequirementChange {
  id: string;
  type: 'addition' | 'modification' | 'deletion';
  description: string;
  rationale: string;
  impact: string;
  approval: string;
  implemented: string;
}

export interface RequirementApprovalStatus {
  required: boolean;
  approvers: string[];
  status: 'pending' | 'approved' | 'rejected' | 'conditional';
  conditions: string[];
  date: string;
}