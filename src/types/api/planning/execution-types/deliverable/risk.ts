/**
 * Deliverable Risk Types
 * 
 * Types for risk management, mitigation, contingency planning, and monitoring.
 * 
 * Generated by CC - Claude Code
 */

export interface DeliverableRisk {
  riskId: string;
  category: 'technical' | 'resource' | 'schedule' | 'quality' | 'external' | 'regulatory';
  description: string;
  probability: number;
  impact: number;
  riskScore: number;
  mitigation: RiskMitigation;
  contingency: RiskContingency;
  monitoring: RiskMonitoringPlan;
  ownership: RiskOwnership;
  status: 'open' | 'mitigated' | 'closed' | 'realized';
}

export interface RiskMitigation {
  strategy: string;
  actions: MitigationAction[];
  timeline: string;
  resources: string[];
  cost: number;
  effectiveness: number;
}

export interface MitigationAction {
  action: string;
  description: string;
  owner: string;
  timeline: string;
  cost: number;
  status: string;
}

export interface RiskContingency {
  plan: string;
  triggers: string[];
  actions: ContingencyAction[];
  resources: string[];
  cost: number;
  activation: string;
}

export interface ContingencyAction {
  action: string;
  description: string;
  sequence: number;
  resources: string[];
  timeline: string;
}

export interface RiskMonitoringPlan {
  indicators: RiskIndicator[];
  frequency: string;
  methods: string[];
  reporting: string;
  escalation: string;
}

export interface RiskIndicator {
  indicator: string;
  measurement: string;
  threshold: number;
  frequency: string;
  owner: string;
}

export interface RiskOwnership {
  owner: string;
  monitor: string;
  escalation: string;
  authority: string;
}

// Risk Forecast Types
export interface RiskForecast {
  exposure: number;
  probability: number;
  impact: string;
  mitigation: string;
}