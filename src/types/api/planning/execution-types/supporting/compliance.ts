/**
 * Compliance and Audit Types
 *
 * Types for compliance management and audit tracking.
 *
 * Generated by CC - Claude Code
 */

export interface ComplianceProfile {
  requirements: ComplianceRequirement[];
  status: ComplianceStatus;
  assessments: ComplianceAssessment[];
  violations: ComplianceViolation[];
  remediation: ComplianceRemediation;
}

export interface ComplianceRequirement {
  requirement: string;
  source: string;
  category: string;
  criticality: 'low' | 'medium' | 'high' | 'critical';
  evidence: string[];
  frequency: string;
  owner: string;
}

export interface ComplianceStatus {
  overall: number;
  by_category: CategoryStatus[];
  by_requirement: RequirementStatus[];
  trends: ComplianceTrend[];
  forecast: string;
}

export interface CategoryStatus {
  category: string;
  compliance: number;
  gaps: number;
  risks: string[];
  actions: string[];
}

export interface RequirementStatus {
  requirement: string;
  status: 'compliant' | 'non_compliant' | 'partial' | 'not_assessed';
  evidence: string[];
  gaps: string[];
  actions: string[];
}

export interface ComplianceTrend {
  period: string;
  compliance: number;
  direction: 'improving' | 'stable' | 'declining';
  drivers: string[];
  forecast: string;
}

export interface ComplianceAssessment {
  assessment: string;
  scope: string[];
  methodology: string;
  frequency: string;
  independence: string;
  findings: AssessmentFinding[];
  recommendations: string[];
}

export interface AssessmentFinding {
  finding: string;
  type: 'compliance' | 'gap' | 'risk' | 'opportunity';
  severity: 'low' | 'medium' | 'high' | 'critical';
  evidence: string[];
  impact: string;
  recommendation: string;
}

export interface ComplianceViolation {
  violation: string;
  requirement: string;
  severity: 'minor' | 'major' | 'critical';
  impact: string;
  root_cause: string;
  remediation: string;
  prevention: string[];
}

export interface ComplianceRemediation {
  actions: RemediationAction[];
  timeline: string;
  resources: string[];
  monitoring: string;
  validation: string;
  prevention: string[];
}

export interface RemediationAction {
  action: string;
  type: 'immediate' | 'short_term' | 'long_term';
  priority: 'low' | 'medium' | 'high' | 'critical';
  timeline: string;
  resources: string[];
  owner: string;
  validation: string;
}
