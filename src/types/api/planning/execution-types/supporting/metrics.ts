/**
 * Measurement and Analytics Types
 * 
 * Types for metrics definition and analytics capabilities.
 * 
 * Generated by CC - Claude Code
 */

export interface MetricDefinition {
  metric: string;
  description: string;
  category: string;
  type: 'leading' | 'lagging' | 'concurrent';
  unit: string;
  calculation: string;
  frequency: string;
  targets: MetricTarget[];
  thresholds: MetricThreshold[];
}

export interface MetricTarget {
  target_type: 'minimum' | 'maximum' | 'range' | 'exact';
  value: number;
  period: string;
  rationale: string;
  adjustability: string;
}

export interface MetricThreshold {
  threshold_type: 'warning' | 'critical' | 'escalation';
  value: number;
  action: string;
  escalation: string;
  frequency: string;
}

export interface AnalyticsProfile {
  data_sources: AnalyticsDataSource[];
  processing: AnalyticsProcessing;
  insights: AnalyticsInsight[];
  visualization: AnalyticsVisualization;
  automation: AnalyticsAutomation;
}

export interface AnalyticsDataSource {
  source: string;
  type: 'structured' | 'unstructured' | 'semi_structured';
  volume: string;
  velocity: string;
  variety: string;
  quality: string;
  access: string;
}

export interface AnalyticsProcessing {
  methods: string[];
  tools: string[];
  frequency: string;
  automation: string;
  quality: string;
  scalability: string;
}

export interface AnalyticsInsight {
  insight: string;
  type: 'descriptive' | 'diagnostic' | 'predictive' | 'prescriptive';
  confidence: number;
  impact: string;
  action: string[];
  validation: string;
}

export interface AnalyticsVisualization {
  dashboards: string[];
  reports: string[];
  interactive: string[];
  self_service: string[];
  mobile: string[];
}

export interface AnalyticsAutomation {
  data_collection: string;
  processing: string;
  analysis: string;
  reporting: string;
  alerting: string;
  decision: string;
}