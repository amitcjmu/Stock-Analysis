/**
 * Progress and Performance Types
 * 
 * Types for tracking progress and performance across execution planning.
 * 
 * Generated by CC - Claude Code
 */

export interface ProgressStatus {
  overall: number;
  phases: PhaseProgress[];
  milestones: MilestoneProgress[];
  activities: ActivityProgress[];
  timeline: TimelineProgress;
  quality: QualityProgress;
}

export interface PhaseProgress {
  phase: string;
  progress: number;
  status: 'not_started' | 'in_progress' | 'completed' | 'delayed';
  start_date: string;
  end_date: string;
  forecast: string;
  issues: string[];
}

export interface MilestoneProgress {
  milestone: string;
  status: 'pending' | 'achieved' | 'missed' | 'at_risk';
  date: string;
  variance: number;
  criteria: string[];
  evidence: string;
}

export interface ActivityProgress {
  activity: string;
  progress: number;
  status: string;
  effort: EffortProgress;
  quality: QualityProgress;
  issues: string[];
}

export interface EffortProgress {
  planned: number;
  actual: number;
  remaining: number;
  variance: number;
  efficiency: number;
}

export interface TimelineProgress {
  planned: number;
  actual: number;
  variance: number;
  critical: boolean;
  forecast: string;
  acceleration: string[];
}

export interface QualityProgress {
  score: number;
  reviews: number;
  tests: number;
  defects: number;
  compliance: number;
  trends: ProgressTrend[];
}

export interface ProgressTrend {
  period: string;
  metric: string;
  value: number;
  direction: 'improving' | 'stable' | 'declining';
  forecast: number;
}