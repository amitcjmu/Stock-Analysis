/**
 * Integration and Interface Types
 * 
 * Types for system integration and interface management.
 * 
 * Generated by CC - Claude Code
 */

export interface IntegrationProfile {
  systems: IntegratedSystem[];
  interfaces: SystemInterface[];
  data_flows: DataFlow[];
  dependencies: IntegrationDependency[];
  governance: IntegrationGovernance;
}

export interface IntegratedSystem {
  system: string;
  type: 'source' | 'target' | 'intermediary';
  role: string;
  criticality: 'low' | 'medium' | 'high' | 'critical';
  availability: string;
  performance: string;
}

export interface SystemInterface {
  interface: string;
  protocol: string;
  format: string;
  frequency: string;
  volume: string;
  security: string[];
  monitoring: string;
}

export interface DataFlow {
  flow: string;
  source: string;
  target: string;
  volume: string;
  frequency: string;
  latency: string;
  quality: string;
  security: string[];
}

export interface IntegrationDependency {
  dependency: string;
  type: 'technical' | 'data' | 'process' | 'business';
  criticality: string;
  impact: string;
  mitigation: string;
  monitoring: string;
}

export interface IntegrationGovernance {
  standards: string[];
  policies: string[];
  approval: string;
  change_control: string;
  monitoring: string;
  compliance: string;
}