/**
 * Assignment Optimization Types
 *
 * Type definitions for resource optimization and constraints.
 *
 * Generated by CC - Claude Code
 */

export interface AssignmentOptimization {
  scope: OptimizationScope;
  objectives: OptimizationObjective[];
  constraints: AssignmentOptimizationConstraint[];
  methods: OptimizationMethod[];
  implementation: OptimizationImplementation;
  monitoring: OptimizationMonitoring;
}

export interface OptimizationScope {
  dimensions: string[];
  boundaries: string[];
  inclusions: string[];
  exclusions: string[];
  priorities: string[];
}

export interface OptimizationObjective {
  objective: string;
  type: 'maximize' | 'minimize' | 'target';
  priority: 'high' | 'medium' | 'low';
  measurement: string;
  target: number;
  weight: number;
}

export interface OptimizationMethod {
  method: string;
  approach: string;
  tools: string[];
  applicability: string[];
  limitations: string[];
  effectiveness: string;
}

export interface OptimizationImplementation {
  phases: string[];
  activities: string[];
  timeline: string;
  resources: string[];
  governance: string;
}

export interface OptimizationMonitoring {
  metrics: string[];
  frequency: string;
  methods: string[];
  reporting: string;
  adjustment: string;
}

// Optimization-specific constraint type
export interface AssignmentOptimizationConstraint {
  constraint: string;
  type: 'hard' | 'soft' | 'preference';
  impact: string;
  workaround: string;
  negotiability: string;
}

// Re-export constraint type for use in other modules
export interface AssignmentConstraint {
  constraint: string;
  category: 'temporal' | 'resource' | 'skill' | 'location' | 'regulatory';
  description: string;
  impact: string;
  workaround: string;
  escalation: string;
}
