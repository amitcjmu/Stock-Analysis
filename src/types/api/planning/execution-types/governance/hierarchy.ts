/**
 * Governance Hierarchy Types
 * 
 * Type definitions for governance hierarchy structures, levels, authority,
 * delegation, escalation paths, and organizational relationships.
 * 
 * Generated by CC - Claude Code
 */

export interface GovernanceHierarchy {
  levels: GovernanceLevel[];
  structure: HierarchyStructure;
  reporting: ReportingLine[];
  delegation: DelegationRule[];
  escalation: EscalationPath[];
  span: HierarchySpan;
  depth: number;
}

export interface GovernanceLevel {
  level: number;
  name: string;
  description: string;
  purpose: string;
  scope: string[];
  authority: LevelAuthority;
  responsibilities: string[];
  reporting: string;
  escalation: string;
}

export interface LevelAuthority {
  decision: DecisionAuthority[];
  approval: ApprovalAuthority[];
  oversight: OversightAuthority[];
  escalation: EscalationAuthority[];
}

export interface DecisionAuthority {
  category: string;
  scope: string[];
  limits: AuthorityLimit[];
  delegation: boolean;
  approval: string;
}

export interface ApprovalAuthority {
  type: string;
  scope: string[];
  limits: AuthorityLimit[];
  process: string;
  escalation: string;
}

export interface OversightAuthority {
  domain: string;
  scope: string[];
  frequency: string;
  reporting: string;
  escalation: string;
}

export interface EscalationAuthority {
  triggers: string[];
  scope: string[];
  process: string;
  timeline: string;
}

export interface AuthorityLimit {
  type: 'financial' | 'temporal' | 'scope' | 'resource' | 'risk';
  value: number;
  unit: string;
  conditions: string[];
  exceptions: string[];
}

export interface HierarchyStructure {
  type: 'matrix' | 'functional' | 'divisional' | 'network' | 'hybrid';
  principles: string[];
  design: StructureDesign;
  flexibility: StructureFlexibility;
  evolution: StructureEvolution;
}

export interface StructureDesign {
  criteria: string[];
  constraints: string[];
  optimization: string[];
  trade_offs: string[];
}

export interface StructureFlexibility {
  adaptability: string;
  reconfiguration: string;
  scaling: string;
  responsiveness: string;
}

export interface StructureEvolution {
  triggers: string[];
  process: string;
  criteria: string[];
  timeline: string;
}

export interface ReportingLine {
  from: string;
  to: string;
  type: 'direct' | 'dotted' | 'functional' | 'matrix';
  frequency: string;
  format: ReportingFormat;
  content: ReportingContent[];
  escalation: string;
}

export interface ReportingFormat {
  medium: 'written' | 'verbal' | 'dashboard' | 'presentation' | 'meeting';
  template: string;
  structure: string[];
  standards: string[];
}

export interface ReportingContent {
  category: string;
  elements: string[];
  metrics: string[];
  frequency: string;
  detail: 'summary' | 'detailed' | 'exception';
}

export interface DelegationRule {
  authority: string;
  from: string;
  to: string;
  scope: string[];
  conditions: DelegationCondition[];
  limits: AuthorityLimit[];
  accountability: string;
  monitoring: string;
}

export interface DelegationCondition {
  condition: string;
  requirement: string;
  validation: string;
  monitoring: string;
}

export interface EscalationPath {
  id: string;
  trigger: EscalationTrigger;
  levels: EscalationLevel[];
  timeline: EscalationTimeline;
  process: EscalationProcess;
  resolution: EscalationResolution;
  communication: EscalationCommunication;
}

export interface EscalationTrigger {
  type: 'automatic' | 'manual' | 'threshold' | 'time_based';
  conditions: TriggerCondition[];
  indicators: TriggerIndicator[];
  rules: TriggerRule[];
}

export interface TriggerCondition {
  condition: string;
  threshold: number;
  measurement: string;
  frequency: string;
}

export interface TriggerIndicator {
  indicator: string;
  measurement: string;
  threshold: number;
  trend: boolean;
}

export interface TriggerRule {
  rule: string;
  logic: string;
  conditions: string[];
  actions: string[];
}

export interface EscalationLevel {
  level: number;
  role: string;
  authority: string[];
  responsibilities: string[];
  timeline: string;
  actions: LevelAction[];
  escalation: string;
}

export interface LevelAction {
  action: string;
  authority: string;
  timeline: string;
  resources: string[];
  communication: string;
}

export interface EscalationTimeline {
  initial: string;
  levels: LevelTimeline[];
  maximum: string;
  extensions: string[];
}

export interface LevelTimeline {
  level: number;
  duration: string;
  conditions: string[];
  extensions: string[];
}

export interface EscalationProcess {
  initiation: ProcessStep[];
  assessment: ProcessStep[];
  decision: ProcessStep[];
  communication: ProcessStep[];
  resolution: ProcessStep[];
  follow_up: ProcessStep[];
}

export interface ProcessStep {
  step: string;
  description: string;
  inputs: string[];
  activities: string[];
  outputs: string[];
  roles: string[];
  timeline: string;
  criteria: string[];
}

export interface EscalationResolution {
  options: ResolutionOption[];
  criteria: ResolutionCriteria[];
  process: string;
  authority: string;
  communication: string;
  follow_up: string;
}

export interface ResolutionOption {
  option: string;
  description: string;
  conditions: string[];
  authority: string;
  timeline: string;
  resources: string[];
}

export interface ResolutionCriteria {
  criteria: string;
  measurement: string;
  threshold: number;
  validation: string;
}

export interface EscalationCommunication {
  stakeholders: string[];
  channels: string[];
  frequency: string;
  content: string[];
  feedback: string;
}

export interface HierarchySpan {
  control: number;
  management: number;
  coordination: number;
  communication: number;
}

export interface GovernanceRelationship {
  from: string;
  to: string;
  type: 'reporting' | 'coordination' | 'collaboration' | 'escalation' | 'consultation';
  frequency: string;
  format: string;
  content: string[];
  governance: RelationshipGovernance;
}

export interface RelationshipGovernance {
  charter: string;
  protocols: string[];
  escalation: string;
  review: string;
  evolution: string;
}