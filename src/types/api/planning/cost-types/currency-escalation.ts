/**
 * Currency and Escalation Types
 *
 * Types for currency management, exchange rates, hedging strategies,
 * cost escalation, and contingency planning.
 *
 * Generated by CC - Claude Code
 */

// Currency Management Types
export interface CurrencyCorrelation {
  currency: string;
  correlation: number;
  significance: string;
}

export interface VolatilityForecast {
  period: string;
  forecast: number;
  confidence: number;
  factors: string[];
}

export interface CurrencyThreshold {
  metric: string;
  threshold: number;
  action: string;
  escalation: string;
}

export interface CurrencyAlert {
  alert: string;
  condition: string;
  severity: string;
  recipients: string[];
}

export interface HedgingInstrument {
  instrument: string;
  type: string;
  coverage: number;
  cost: number;
  effectiveness: number;
}

export interface CurrencyExposure {
  currency: string;
  amount: number;
  type: 'transaction' | 'translation' | 'economic';
  hedging: number;
  netExposure: number;
}

export interface CurrencyVolatility {
  currency: string;
  volatility: number;
  correlation: CurrencyCorrelation[];
  forecast: VolatilityForecast;
}

export interface CurrencyMonitoring {
  frequency: string;
  metrics: string[];
  thresholds: CurrencyThreshold[];
  alerts: CurrencyAlert[];
}

// Approval and Authority Types
export interface ApprovalLevel {
  level: string;
  authority: string;
  threshold: number;
  process: string;
}

export interface ApprovalThreshold {
  metric: string;
  threshold: number;
  level: string;
  escalation: string;
}

export interface ApprovalAuthority {
  authority: string;
  scope: string[];
  limits: AuthorityLimit[];
  delegation: string;
}

export interface ApprovalProcess {
  steps: ProcessStep[];
  timeline: string;
  escalation: string;
  documentation: string;
}

export interface AuthorityLimit {
  type: string;
  limit: number;
  conditions: string[];
}

export interface ProcessStep {
  step: string;
  owner: string;
  duration: number;
  criteria: string[];
}
