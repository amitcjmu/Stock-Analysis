/**
 * Cost Optimization and Benchmarking Types
 *
 * Types for cost optimization analysis, opportunity identification,
 * and benchmarking against internal and external standards.
 *
 * Generated by CC - Claude Code
 */

import type { ConfigurationValue } from '@/types/shared/config-types';

// Forward declarations for types from other modules
export type CostOptimizationResult = Record<string, unknown>;
export type CostSaving = Record<string, unknown>;
export type CostAlternative = Record<string, unknown>;
export type CostRecommendation = Record<string, unknown>;
export type CostTradeoff = Record<string, unknown>;

// Optimization Supporting Types
export interface CostOptimization {
  current: OptimizationState;
  targets: OptimizationTarget[];
  opportunities: OptimizationOpportunity[];
  actions: OptimizationAction[];
  constraints: OptimizationConstraint[];
  results: OptimizationResult[];
  tradeoffs: OptimizationTradeoff[];
}

export interface OptimizationState {
  totalCost: number;
  efficiency: number;
  utilization: number;
  waste: number;
  opportunities: number;
  constraints: number;
}

export interface OptimizationTarget {
  metric: string;
  current: number;
  target: number;
  improvement: number;
  timeline: string;
  feasibility: string;
}

export interface OptimizationOpportunity {
  opportunity: string;
  category: string;
  saving: number;
  effort: string;
  risk: string;
  timeline: string;
  feasibility: string;
}

export interface OptimizationAction {
  action: string;
  description: string;
  category: string;
  impact: ActionImpact;
  effort: ActionEffort;
  timeline: ActionTimeline;
  dependencies: string[];
}

export interface OptimizationConstraint {
  constraint: string;
  type: string;
  value: ConfigurationValue;
  flexibility: string;
  impact: string;
  workaround: string;
}

export interface OptimizationResult {
  metric: string;
  baseline: number;
  optimized: number;
  improvement: number;
  confidence: number;
  timeline: string;
}

export interface OptimizationTradeoff {
  tradeoff: string;
  option1: TradeoffOption;
  option2: TradeoffOption;
  recommendation: string;
  rationale: string;
}

// Optimization Request/Response Support Types
export interface CostOptimizationTarget {
  target: string;
  metric: string;
  current: number;
  desired: number;
  timeline: string;
  constraints: string[];
}

// Benchmark Supporting Types
export interface BenchmarkInsight {
  insight: string;
  category: string;
  significance: 'low' | 'medium' | 'high' | 'critical';
  action: string;
}

export interface BenchmarkRecommendation {
  recommendation: string;
  rationale: string;
  priority: 'low' | 'medium' | 'high' | 'critical';
  effort: string;
  timeline: string;
}

// Action Supporting Types
export interface ActionImpact {
  financial: number;
  operational: string;
  strategic: string;
  timeline: string;
}

export interface ActionEffort {
  complexity: 'low' | 'medium' | 'high' | 'very_high';
  duration: number;
  resources: ActionResource[];
  dependencies: string[];
}

export interface ActionTimeline {
  start: string;
  phases: ActionPhase[];
  completion: string;
  milestones: ActionMilestone[];
}

export interface ActionResource {
  resource: string;
  quantity: number;
  duration: number;
  cost: number;
}

export interface ActionPhase {
  phase: string;
  duration: number;
  activities: string[];
  deliverables: string[];
}

export interface ActionMilestone {
  milestone: string;
  date: string;
  criteria: string[];
  deliverable: string;
}

export interface TradeoffOption {
  option: string;
  cost: number;
  benefit: string;
  risk: string;
  timeline: string;
}

// Optimization Results Support Types
export interface SavingsBreakdown {
  category: string;
  amount: number;
  percentage: number;
  timeline: string;
  confidence: number;
}

export interface ImplementationPlan {
  phases: ImplementationPhase[];
  timeline: ImplementationTimeline;
  resources: ImplementationResource[];
  risks: ImplementationRisk[];
}

export interface OptimizationRisk {
  risks: OptimizationRiskFactor[];
  mitigation: OptimizationMitigation[];
  monitoring: OptimizationMonitoring;
}

export interface OptimizationTimeline {
  totalDuration: number;
  phases: OptimizationPhase[];
  milestones: OptimizationMilestone[];
  dependencies: OptimizationDependency[];
}

// Implementation Supporting Types
export interface ImplementationPhase {
  phase: string;
  duration: number;
  activities: string[];
  resources: string[];
}

export interface ImplementationTimeline {
  totalDuration: number;
  start: string;
  end: string;
  milestones: string[];
}

export interface ImplementationResource {
  resource: string;
  quantity: number;
  cost: number;
  duration: number;
}

export interface ImplementationRisk {
  risk: string;
  probability: number;
  impact: number;
  mitigation: string;
}

export interface OptimizationRiskFactor {
  risk: string;
  probability: number;
  impact: number;
  category: string;
}

export interface OptimizationMitigation {
  risk: string;
  strategy: string;
  cost: number;
  effectiveness: number;
}

export interface OptimizationMonitoring {
  frequency: string;
  metrics: string[];
  thresholds: string[];
  escalation: string[];
}

export interface OptimizationPhase {
  phase: string;
  duration: number;
  activities: string[];
  deliverables: string[];
}

export interface OptimizationMilestone {
  milestone: string;
  date: string;
  criteria: string[];
  benefit: number;
}

export interface OptimizationDependency {
  from: string;
  to: string;
  type: string;
  impact: string;
}
