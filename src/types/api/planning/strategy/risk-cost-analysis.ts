/**
 * Strategy Risk and Cost Analysis Types
 * 
 * Risk analysis, cost analysis, mitigation strategies, and funding options.
 * 
 * Generated by CC - Claude Code
 */

// Risk Analysis
export interface StrategyRiskAnalysis {
  risks: StrategyRisk[];
  riskScore: number;
  riskLevel: 'low' | 'medium' | 'high' | 'critical';
  mitigationStrategies: RiskMitigation[];
  contingencyPlans: ContingencyPlan[];
  probabilityOfSuccess: number;
}

export interface StrategyRisk {
  id: string;
  name: string;
  description: string;
  category: string;
  probability: number;
  impact: number;
  riskScore: number;
  mitigation: string;
}

export interface RiskMitigation {
  riskId: string;
  strategy: string;
  actions: string[];
  owner: string;
  timeline: string;
  cost: number;
}

export interface ContingencyPlan {
  id: string;
  trigger: string;
  actions: string[];
  resources: string[];
  timeline: string;
}

// Cost Analysis
export interface StrategyCostAnalysis {
  totalCost: number;
  costBreakdown: CostBreakdown[];
  costVariances: CostVariance[];
  costSensitivity: CostSensitivity[];
  fundingOptions: FundingOption[];
  paymentSchedule: PaymentSchedule[];
}

export interface CostBreakdown {
  category: string;
  amount: number;
  percentage: number;
  confidence: number;
}

export interface CostVariance {
  category: string;
  baseline: number;
  optimistic: number;
  pessimistic: number;
  expected: number;
}

export interface CostSensitivity {
  parameter: string;
  impact: number;
  elasticity: number;
}

export interface FundingOption {
  option: string;
  amount: number;
  terms: string;
  cost: number;
  availability: string;
}

export interface PaymentSchedule {
  milestone: string;
  amount: number;
  date: string;
  conditions: string[];
}