/**
 * Report Styling & Layout Types
 * 
 * Type definitions for report styling, colors, fonts, layouts,
 * spacing, and chart styling configurations.
 * 
 * Generated by CC - Claude Code
 */

// Core Styling Types
export interface ReportStyling {
  theme: string;
  colors: ColorScheme;
  fonts: FontScheme;
  layout: LayoutScheme;
  spacing: SpacingScheme;
  charts: ChartStyling;
}

// Color Scheme Types
export interface ColorScheme {
  primary: string;
  secondary: string;
  accent: string;
  background: string;
  text: string;
  borders: string;
}

// Font Scheme Types
export interface FontScheme {
  heading: FontDefinition;
  body: FontDefinition;
  caption: FontDefinition;
  code: FontDefinition;
}

export interface FontDefinition {
  family: string;
  size: number;
  weight: string;
  style: string;
  color: string;
}

// Layout Scheme Types
export interface LayoutScheme {
  margins: LayoutMargins;
  columns: LayoutColumns;
  spacing: LayoutSpacing;
  alignment: LayoutAlignment;
}

export interface LayoutMargins {
  top: number;
  bottom: number;
  left: number;
  right: number;
}

export interface LayoutColumns {
  count: number;
  width: number[];
  gap: number;
  balance: boolean;
}

export interface LayoutSpacing {
  paragraph: number;
  section: number;
  element: number;
}

export interface LayoutAlignment {
  horizontal: 'left' | 'center' | 'right' | 'justify';
  vertical: 'top' | 'middle' | 'bottom';
}

// Spacing Scheme Types
export interface SpacingScheme {
  sections: number;
  paragraphs: number;
  lists: number;
  tables: number;
  figures: number;
}

// Chart Styling Types
export interface ChartStyling {
  colors: ChartColors;
  fonts: ChartFonts;
  layout: ChartLayout;
  legends: ChartLegend;
  axes: ChartAxes;
}

export interface ChartColors {
  series: string[];
  background: string;
  grid: string;
  text: string;
}

export interface ChartFonts {
  title: FontDefinition;
  axis: FontDefinition;
  legend: FontDefinition;
  data: FontDefinition;
}

export interface ChartLayout {
  width: number;
  height: number;
  margins: LayoutMargins;
  padding: number;
}

export interface ChartLegend {
  position: 'top' | 'bottom' | 'left' | 'right' | 'none';
  style: LegendStyle;
  interaction: boolean;
}

export interface LegendStyle {
  font: FontDefinition;
  spacing: number;
  alignment: string;
}

export interface ChartAxes {
  x: AxisStyle;
  y: AxisStyle;
  grid: GridStyle;
}

export interface AxisStyle {
  font: FontDefinition;
  color: string;
  width: number;
  ticks: TickStyle;
}

export interface GridStyle {
  color: string;
  width: number;
  style: 'solid' | 'dashed' | 'dotted';
}

export interface TickStyle {
  color: string;
  length: number;
  width: number;
}