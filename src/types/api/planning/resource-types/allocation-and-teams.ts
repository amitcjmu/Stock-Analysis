/**
 * Resource Allocation and Teams Module
 *
 * Types for resource allocation, team management, team members,
 * capacity management, and team performance tracking.
 *
 * Generated by CC - Claude Code
 */

import type { FlexibilityLevel } from './supporting-types';

// Import dependencies from core data types
export type { ResourceAllocation, AllocationConstraint } from './core-data-types';

export interface ResourceTeam {
  id: string;
  name: string;
  type: 'internal' | 'external' | 'hybrid';
  size: number;
  lead: TeamMember;
  members: TeamMember[];
  skills: TeamSkill[];
  capacity: TeamCapacity;
  cost: TeamCost;
  performance: TeamPerformance;
  availability: TeamAvailability[];
  location: TeamLocation;
}

export interface TeamMember {
  id: string;
  name: string;
  role: string;
  level: string;
  skills: MemberSkill[];
  availability: number;
  cost: number;
  location: string;
  performance: MemberPerformance;
}

export interface TeamSkill {
  skill: string;
  level: string;
  count: number;
  availability: number;
  development: boolean;
}

export interface TeamCapacity {
  totalCapacity: number;
  availableCapacity: number;
  allocatedCapacity: number;
  efficiency: number;
  utilization: number;
}

export interface TeamCost {
  totalCost: number;
  costPerMember: number;
  overhead: number;
  currency: string;
  model: string;
}

export interface TeamPerformance {
  productivity: number;
  quality: number;
  velocity: number;
  satisfaction: number;
  retention: number;
}

export interface TeamAvailability {
  startDate: string;
  endDate: string;
  capacity: number;
  members: number;
}

export interface TeamLocation {
  primary: string;
  secondary: string[];
  distributed: boolean;
  timezone: string;
}

export interface MemberSkill {
  skill: string;
  level: string;
  certified: boolean;
  experience: number;
  proficiency: number;
}

export interface MemberPerformance {
  productivity: number;
  quality: number;
  reliability: number;
  collaboration: number;
  adaptability: number;
}

export interface GapResolutionOption {
  option: 'hire' | 'train' | 'contract' | 'outsource' | 'reallocate';
  cost: number;
  timeline: string;
  risk: string;
  feasibility: string;
  impact: string;
}

export interface RedeploymentOption {
  opportunity: string;
  utilization: number;
  benefit: string;
  effort: string;
  timeline: string;
}

export interface DevelopmentPlan {
  method: string;
  duration: number;
  milestones: string[];
  resources: string[];
  cost: number;
  success: number;
}
