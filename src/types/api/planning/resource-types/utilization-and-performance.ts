/**
 * Utilization and Performance Module
 *
 * Types for resource utilization tracking, performance metrics,
 * trend analysis, projections, and utilization optimization.
 *
 * Generated by CC - Claude Code
 */

// Import skill-related types that are used here
export type { SkillUtilization, SkillCostDetail } from './skills-management';

export interface UtilizationAnalysis {
  overallUtilization: number;
  byResource: ResourceUtilization[];
  bySkill: SkillUtilization[];
  byPeriod: PeriodUtilization[];
  byLocation: LocationUtilization[];
  trends: UtilizationTrend[];
  projections: UtilizationProjection[];
}

export interface SkillsAnalysis {
  coverage: SkillCoverage[];
  gaps: SkillGap[];
  redundancy: SkillRedundancy[];
  development: SkillDevelopmentNeeds[];
  risks: SkillRisk[];
  opportunities: SkillOpportunity[];
}

// Utilization Supporting Types
export interface ResourceUtilization {
  resourceId: string;
  resourceName: string;
  type: string;
  utilization: number;
  capacity: number;
  allocated: number;
  efficiency: number;
  cost: number;
  performance: number;
}

export interface PeriodUtilization {
  period: string;
  utilization: number;
  capacity: number;
  demand: number;
  variance: number;
  trend: string;
}

export interface LocationUtilization {
  location: string;
  utilization: number;
  capacity: number;
  cost: number;
  efficiency: number;
  satisfaction: number;
}

export interface UtilizationTrend {
  metric: string;
  trend: 'increasing' | 'decreasing' | 'stable';
  rate: number;
  confidence: number;
  factors: string[];
}

export interface UtilizationProjection {
  period: string;
  projected: number;
  confidence: number;
  assumptions: string[];
  risks: string[];
}

export interface SkillCoverage {
  skill: string;
  level: string;
  coverage: number;
  required: number;
  available: number;
  redundancy: number;
}

export interface SkillGap {
  skill: string;
  level: string;
  required: number;
  available: number;
  gap: number;
  impact: 'low' | 'medium' | 'high' | 'critical';
  options: GapResolutionOption[];
}

export interface SkillRedundancy {
  skill: string;
  level: string;
  redundancy: number;
  utilization: number;
  cost: number;
  optimization: string;
}

export interface SkillDevelopmentNeeds {
  skill: string;
  level: string;
  need: number;
  urgency: 'low' | 'medium' | 'high' | 'critical';
  method: string;
  timeline: string;
}

export interface SkillRisk {
  skill: string;
  risk: string;
  probability: number;
  impact: number;
  mitigation: string;
  contingency: string;
}

export interface SkillOpportunity {
  skill: string;
  opportunity: string;
  benefit: string;
  effort: string;
  timeline: string;
  investment: number;
}

export interface GapResolutionOption {
  option: 'hire' | 'train' | 'contract' | 'outsource' | 'reallocate';
  cost: number;
  timeline: string;
  risk: string;
  feasibility: string;
  impact: string;
}
