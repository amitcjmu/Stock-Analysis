/**
 * Skills Management Module
 * 
 * Types for skills management including skill matrices, gap analysis,
 * skill development, acquisition planning, and skills-related analytics.
 * 
 * Generated by CC - Claude Code
 */

// Import dependencies from core types
export type { SkillRequirement, SkillDevelopmentOption } from './core-data-types';

export interface SkillMatrix {
  skills: Skill[];
  levels: SkillLevel[];
  requirements: SkillMatrixEntry[];
  availability: SkillAvailability[];
  gaps: SkillGap[];
  development: SkillDevelopment[];
}

export interface Skill {
  id: string;
  name: string;
  category: string;
  description: string;
  level: SkillLevel[];
  certifications: string[];
  alternatives: string[];
}

export interface SkillLevel {
  level: string;
  name: string;
  description: string;
  experience: number;
  capabilities: string[];
  assessment: string;
}

export interface SkillMatrixEntry {
  skill: string;
  level: string;
  required: number;
  available: number;
  gap: number;
  priority: string;
}

export interface SkillAvailability {
  skill: string;
  level: string;
  available: number;
  allocated: number;
  remaining: number;
  cost: number;
}

export interface SkillGap {
  skill: string;
  level: string;
  required: number;
  available: number;
  gap: number;
  impact: 'low' | 'medium' | 'high' | 'critical';
  options: GapResolutionOption[];
}

export interface SkillDevelopment {
  skill: string;
  currentLevel: string;
  targetLevel: string;
  method: 'training' | 'mentoring' | 'certification' | 'experience';
  duration: number;
  cost: number;
  success: number;
}

export interface SkillsGapAnalysis {
  currentSkills: SkillInventory[];
  requiredSkills: SkillRequirement[];
  gaps: SkillGap[];
  surpluses: SkillSurplus[];
  development: SkillDevelopmentPlan[];
  acquisition: SkillAcquisitionPlan[];
  timeline: SkillTimeline[];
}

export interface SkillInventory {
  resourceId: string;
  skill: string;
  level: string;
  certified: boolean;
  experience: number;
  availability: number;
}

export interface SkillSurplus {
  skill: string;
  level: string;
  surplus: number;
  utilization: number;
  redeployment: RedeploymentOption[];
}

export interface SkillDevelopmentPlan {
  resourceId: string;
  skill: string;
  currentLevel: string;
  targetLevel: string;
  plan: DevelopmentPlan;
  timeline: string;
  cost: number;
}

export interface SkillAcquisitionPlan {
  skill: string;
  level: string;
  quantity: number;
  method: 'hire' | 'contract' | 'train' | 'outsource';
  timeline: string;
  cost: number;
  success: number;
}

export interface SkillTimeline {
  period: string;
  required: SkillRequirement[];
  available: SkillAvailability[];
  gaps: SkillGap[];
  actions: SkillAction[];
}

export interface SkillsAnalysis {
  coverage: SkillCoverage[];
  gaps: SkillGap[];
  redundancy: SkillRedundancy[];
  development: SkillDevelopmentNeeds[];
  risks: SkillRisk[];
  opportunities: SkillOpportunity[];
}

export interface SkillCoverage {
  skill: string;
  level: string;
  coverage: number;
  required: number;
  available: number;
  redundancy: number;
}

export interface SkillRedundancy {
  skill: string;
  level: string;
  redundancy: number;
  utilization: number;
  cost: number;
  optimization: string;
}

export interface SkillDevelopmentNeeds {
  skill: string;
  level: string;
  need: number;
  urgency: 'low' | 'medium' | 'high' | 'critical';
  method: string;
  timeline: string;
}

export interface SkillRisk {
  skill: string;
  risk: string;
  probability: number;
  impact: number;
  mitigation: string;
  contingency: string;
}

export interface SkillOpportunity {
  skill: string;
  opportunity: string;
  benefit: string;
  effort: string;
  timeline: string;
  investment: number;
}

export interface SkillUtilization {
  skill: string;
  level: string;
  utilization: number;
  demand: number;
  supply: number;
  cost: number;
  premium: number;
}

export interface SkillCostDetail {
  skill: string;
  level: string;
  cost: number;
  rate: number;
  premium: number;
  demand: number;
  supply: number;
}

export interface SkillAction {
  action: 'develop' | 'acquire' | 'reallocate' | 'outsource';
  skill: string;
  quantity: number;
  timeline: string;
  cost: number;
  risk: string;
}

export interface GapResolutionOption {
  option: 'hire' | 'train' | 'contract' | 'outsource' | 'reallocate';
  cost: number;
  timeline: string;
  risk: string;
  feasibility: string;
  impact: string;
}

export interface RedeploymentOption {
  opportunity: string;
  utilization: number;
  benefit: string;
  effort: string;
  timeline: string;
}

export interface DevelopmentPlan {
  method: string;
  duration: number;
  milestones: string[];
  resources: string[];
  cost: number;
  success: number;
}