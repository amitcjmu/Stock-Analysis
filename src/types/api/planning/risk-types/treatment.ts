/**
 * Risk Treatment and Mitigation Types
 * 
 * Type definitions for risk treatment strategies, mitigation planning,
 * and comprehensive risk response management.
 * 
 * Generated by CC - Claude Code
 */

import type {
  ActionCost,
  ActionEffort,
  ActionTimeline,
  ActionResource,
  ActionDependency,
  ActionEffectiveness,
  ActionMilestone
} from './utilities';

// Mitigation Planning Types
export interface ComprehensiveMitigationPlan {
  strategies: MitigationStrategy[];
  actions: MitigationAction[];
  controls: MitigationControl[];
  timeline: MitigationTimeline;
  budget: MitigationBudget;
  resources: MitigationResource[];
  governance: MitigationGovernance;
  monitoring: MitigationMonitoring;
}

export interface MitigationStrategy {
  strategy: 'avoid' | 'mitigate' | 'transfer' | 'accept';
  description: string;
  applicability: string[];
  effectiveness: number;
  cost: number;
  timeline: string;
}

export interface MitigationAction {
  action: string;
  type: string;
  description: string;
  owner: string;
  deadline: string;
  status: 'planned' | 'in_progress' | 'completed' | 'overdue';
  effectiveness: number;
}

export interface MitigationControl {
  control: string;
  type: string;
  description: string;
  frequency: string;
  automation: string;
  effectiveness: number;
  cost: number;
}

export interface MitigationTimeline {
  phases: MitigationPhase[];
  milestones: MitigationMilestone[];
  dependencies: string[];
  critical_path: string[];
}

export interface MitigationBudget {
  total: number;
  byStrategy: StrategyBudget[];
  byCategory: CategoryBudget[];
  contingency: number;
  currency: string;
}

export interface MitigationResource {
  resource: string;
  type: string;
  quantity: number;
  cost: number;
  availability: string;
}

export interface MitigationGovernance {
  oversight: string;
  reporting: string;
  approval: string;
  escalation: string;
}

export interface MitigationMonitoring {
  indicators: string[];
  frequency: string;
  reporting: string;
  thresholds: string[];
}

// Risk Action Types
export interface RiskAction {
  action: string;
  type: 'preventive' | 'corrective' | 'detective' | 'directive';
  priority: 'low' | 'medium' | 'high' | 'critical';
  urgency: 'immediate' | 'short_term' | 'medium_term' | 'long_term';
  cost: ActionCost;
  effort: ActionEffort;
  timeline: ActionTimeline;
  resources: ActionResource[];
  dependencies: ActionDependency[];
  effectiveness: ActionEffectiveness;
}

// Supporting Detail Types
export interface MitigationPhase {
  phase: string;
  start: string;
  end: string;
  activities: string[];
  deliverables: string[];
}

export interface MitigationMilestone {
  milestone: string;
  date: string;
  criteria: string[];
  dependencies: string[];
}

export interface StrategyBudget {
  strategy: string;
  budget: number;
  allocated: number;
  remaining: number;
}

export interface CategoryBudget {
  category: string;
  budget: number;
  allocated: number;
  remaining: number;
}