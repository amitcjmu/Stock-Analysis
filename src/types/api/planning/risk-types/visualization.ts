/**
 * Risk Visualization Types
 *
 * Type definitions for risk visualization including risk matrices,
 * heat maps, charts, and visual representation of risk data.
 *
 * Generated by CC - Claude Code
 */

// Risk Matrix Types
export interface RiskMatrix {
  dimensions: MatrixDimension[];
  cells: MatrixCell[];
  zones: RiskZone[];
  thresholds: MatrixThreshold[];
  colors: MatrixColor[];
  legend: MatrixLegend;
}

export interface MatrixDimension {
  dimension: 'probability' | 'impact';
  scale: string[];
  description: string[];
}

export interface MatrixCell {
  probability: string;
  impact: string;
  level: 'low' | 'medium' | 'high' | 'critical';
  color: string;
  action: string;
}

export interface RiskZone {
  zone: string;
  level: 'low' | 'medium' | 'high' | 'critical';
  color: string;
  description: string;
  action: string;
}

export interface MatrixThreshold {
  from: string;
  to: string;
  level: string;
  action: string;
}

export interface MatrixColor {
  level: string;
  color: string;
  description: string;
}

export interface MatrixLegend {
  items: LegendItem[];
  position: string;
  format: string;
}

export interface LegendItem {
  label: string;
  color: string;
  description: string;
}

// Heat Map Types
export interface RiskHeatMap {
  risks: HeatMapRisk[];
  zones: HeatMapZone[];
  intensity: IntensityScale;
  filters: HeatMapFilter[];
  aggregation: HeatMapAggregation;
  visualization: HeatMapVisualization;
}

export interface HeatMapRisk {
  riskId: string;
  name: string;
  category: string;
  probability: number;
  impact: number;
  level: string;
  position: HeatMapPosition;
}

export interface HeatMapZone {
  zone: string;
  boundaries: ZoneBoundary[];
  color: string;
  action: string;
}

export interface IntensityScale {
  minimum: number;
  maximum: number;
  colors: string[];
  gradation: number;
}

export interface HeatMapFilter {
  filter: string;
  values: string[];
  active: boolean;
}

export interface HeatMapAggregation {
  method: 'count' | 'sum' | 'average' | 'weighted';
  dimension: string;
  groups: string[];
}

export interface HeatMapVisualization {
  type: 'bubble' | 'scatter' | 'grid' | 'contour';
  size: 'probability' | 'impact' | 'velocity' | 'count';
  color: 'level' | 'category' | 'trend';
  opacity: number;
}

export interface HeatMapPosition {
  x: number;
  y: number;
  size: number;
  color: string;
}

export interface ZoneBoundary {
  x_min: number;
  x_max: number;
  y_min: number;
  y_max: number;
}
