/**
 * Slack Analysis Types
 *
 * Task and phase slack analysis, slack distribution, and risk areas.
 *
 * Generated by CC - Claude Code
 */

// Slack Analysis
export interface SlackAnalysis {
  totalSlack: number;
  freeSlack: number;
  tasks: TaskSlack[];
  phases: PhaseSlack[];
  criticalPath: boolean;
  slackDistribution: SlackDistribution;
  riskAreas: SlackRiskArea[];
}

export interface TaskSlack {
  taskId: string;
  totalSlack: number;
  freeSlack: number;
  independentSlack: number;
  criticality: 'critical' | 'near_critical' | 'normal';
}

export interface PhaseSlack {
  phaseId: string;
  totalSlack: number;
  averageSlack: number;
  criticalTasks: number;
  totalTasks: number;
}

export interface SlackDistribution {
  ranges: SlackRange[];
  mean: number;
  median: number;
  standardDeviation: number;
}

export interface SlackRange {
  min: number;
  max: number;
  count: number;
  percentage: number;
}

export interface SlackRiskArea {
  area: string;
  riskLevel: 'low' | 'medium' | 'high' | 'critical';
  description: string;
  impact: string;
  mitigation: string;
}
