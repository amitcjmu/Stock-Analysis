/**
 * Timeline Planning and Scheduling Types
 * 
 * Planning horizons, working calendars, and resource scheduling.
 * 
 * Generated by CC - Claude Code
 */

// Planning and Scheduling Types
export interface PlanningHorizon {
  startDate: string;
  endDate: string;
  duration: number;
  workingDays: number;
  holidays: Holiday[];
  workingCalendar: WorkingCalendar;
  timeZone: string;
  granularity: 'hour' | 'day' | 'week' | 'month';
}

export interface WorkingCalendar {
  name: string;
  workingDays: string[];
  workingHours: WorkingHours;
  holidays: Holiday[];
  exceptions: CalendarException[];
  timeZone: string;
}

export interface WorkingHours {
  monday: DaySchedule;
  tuesday: DaySchedule;
  wednesday: DaySchedule;
  thursday: DaySchedule;
  friday: DaySchedule;
  saturday: DaySchedule;
  sunday: DaySchedule;
}

export interface DaySchedule {
  working: boolean;
  startTime: string;
  endTime: string;
  breaks: Break[];
  totalHours: number;
}

export interface Break {
  startTime: string;
  endTime: string;
  duration: number;
  paid: boolean;
}

export interface Holiday {
  date: string;
  name: string;
  type: 'national' | 'regional' | 'company';
  recurring: boolean;
}

export interface CalendarException {
  date: string;
  type: 'working' | 'non_working' | 'half_day';
  hours?: DaySchedule;
  reason: string;
}

// Resource Planning
export interface ResourceAvailability {
  resourceId: string;
  resourceName: string;
  type: string;
  capacity: number;
  periods: AvailabilityPeriod[];
  constraints: string[];
  cost: number;
}

export interface AvailabilityPeriod {
  startDate: string;
  endDate: string;
  capacity: number;
  available: boolean;
  reason?: string;
}

export interface ResourceAvailabilityPeriod {
  startDate: string;
  endDate: string;
  capacity: number;
  allocated: number;
  available: number;
  constraints: string[];
}

export interface ResourceCost {
  hourlyRate: number;
  dailyRate: number;
  fixedCost: number;
  currency: string;
  billingModel: 'hourly' | 'daily' | 'fixed' | 'milestone';
}

export interface ResourceConstraint {
  type: 'availability' | 'skill' | 'location' | 'cost' | 'capacity';
  description: string;
  impact: 'low' | 'medium' | 'high' | 'critical';
  startDate?: string;
  endDate?: string;
  value?: unknown;
}

export interface ResourceAssignment {
  taskId: string;
  startDate: string;
  endDate: string;
  allocation: number;
  role: string;
  responsibility: string;
}

export interface TimelineResource {
  id: string;
  name: string;
  type: 'human' | 'equipment' | 'material' | 'facility' | 'external';
  capacity: number;
  availability: ResourceAvailabilityPeriod[];
  cost: ResourceCost;
  skills: string[];
  location: string;
  calendar: WorkingCalendar;
  constraints: ResourceConstraint[];
  assignments: ResourceAssignment[];
}