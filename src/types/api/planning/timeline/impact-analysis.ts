/**
 * Timeline Impact Analysis Types
 *
 * Impact analysis for timeline changes including milestone and risk impacts.
 *
 * Generated by CC - Claude Code
 */

// Timeline Impact Analysis
export interface TimelineImpactAnalysis {
  scope: 'task' | 'phase' | 'project';
  affectedItems: string[];
  durationImpact: number;
  costImpact: number;
  resourceImpact: ResourceImpactDetail[];
  milestoneImpact: MilestoneImpact[];
  riskImpact: RiskImpactDetail[];
  recommendations: ImpactRecommendation[];
}

export interface MilestoneImpact {
  milestoneId: string;
  milestoneName: string;
  currentDate: string;
  newDate: string;
  delay: number;
  impact: 'low' | 'medium' | 'high' | 'critical';
  stakeholders: string[];
}

export interface RiskImpactDetail {
  riskId: string;
  riskDescription: string;
  currentProbability: number;
  newProbability: number;
  currentImpact: number;
  newImpact: number;
  mitigation: string;
}

export interface ImpactRecommendation {
  type: string;
  description: string;
  priority: 'low' | 'medium' | 'high' | 'critical';
  effort: string;
  benefit: string;
  risk: string;
}

export interface ResourceImpactDetail {
  resourceId: string;
  resourceName: string;
  impactType: string;
  magnitude: number;
  duration: number;
  cost: number;
  mitigation: string;
}
