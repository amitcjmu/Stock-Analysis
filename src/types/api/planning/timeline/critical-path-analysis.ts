/**
 * Critical Path Analysis Types
 * 
 * Critical path analysis, float analysis, bottlenecks, and optimization opportunities.
 * 
 * Generated by CC - Claude Code
 */

// Critical Path Analysis
export interface CriticalPathAnalysis {
  criticalPath: string[];
  totalDuration: number;
  criticalTasks: CriticalTask[];
  nearCriticalPaths: NearCriticalPath[];
  floatAnalysis: FloatAnalysis;
  bottlenecks: Bottleneck[];
  optimizationOpportunities: PathOptimization[];
}

export interface CriticalTask {
  taskId: string;
  taskName: string;
  startDate: string;
  endDate: string;
  duration: number;
  totalFloat: number;
  freeFloat: number;
  criticality: number;
}

export interface NearCriticalPath {
  pathId: string;
  tasks: string[];
  totalFloat: number;
  duration: number;
  riskLevel: 'low' | 'medium' | 'high';
}

export interface FloatAnalysis {
  totalFloat: TaskFloat[];
  freeFloat: TaskFloat[];
  independentFloat: TaskFloat[];
  interfering: TaskFloat[];
}

export interface TaskFloat {
  taskId: string;
  float: number;
  type: 'total' | 'free' | 'independent' | 'interfering';
  impact: string;
}

export interface Bottleneck {
  type: 'resource' | 'dependency' | 'constraint';
  id: string;
  description: string;
  impact: number;
  duration: number;
  affectedTasks: string[];
  recommendations: string[];
}

export interface PathOptimization {
  type: 'fast_track' | 'crash' | 'parallel' | 'reschedule';
  description: string;
  savings: number;
  cost: number;
  risk: string;
  feasibility: string;
}