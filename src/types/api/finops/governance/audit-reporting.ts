/**
 * Audit and Reporting Types
 * 
 * Audit trail management, compliance reporting, and governance analytics.
 * 
 * Generated by CC - Claude Code
 */

// Audit Trail Types
export interface AuditTrail {
  entries: AuditEntry[];
  retention: string;
  compliance: AuditCompliance;
}

export interface AuditEntry {
  id: string;
  timestamp: string;
  user: string;
  action: string;
  resource: string;
  details: Record<string, any>;
  outcome: 'success' | 'failure' | 'partial';
  impact: string;
}

export interface AuditCompliance {
  standards: string[];
  requirements: string[];
  certifications: string[];
  reports: AuditReport[];
}

export interface AuditReport {
  id: string;
  type: string;
  period: string;
  status: string;
  findings: string[];
  recommendations: string[];
  generatedAt: string;
}