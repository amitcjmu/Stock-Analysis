/**
 * FinOps Governance Types
 *
 * Type definitions for approval workflows, stakeholders, escalation paths, and governance policies.
 * Covers all governance-related data structures for FinOps flows.
 *
 * Generated by CC - Modularized from flow-management.ts
 */

export interface ApprovalWorkflow {
  id: string;
  name: string;
  type: 'sequential' | 'parallel' | 'conditional';
  thresholds: ApprovalThreshold[];
  approvers: Approver[];
  escalation: EscalationRule[];
}

export interface ApprovalThreshold {
  metric: string;
  operator: string;
  value: number;
  level: string;
}

export interface Approver {
  role: string;
  userId?: string;
  delegateId?: string;
  level: number;
}

export interface EscalationRule {
  condition: string;
  escalateTo: string;
  timeframe: string;
}

export interface Stakeholder {
  id: string;
  name: string;
  role: string;
  email: string;
  responsibility: string;
  notificationPreferences: NotificationPreference[];
}

export interface NotificationPreference {
  type: string;
  channel: 'email' | 'sms' | 'slack' | 'teams';
  frequency: string;
  enabled: boolean;
}

export interface EscalationPath {
  level: number;
  role: string;
  threshold: string;
  timeframe: string;
  action: string;
}

export interface GovernanceStatus {
  complianceScore: number;
  policyViolations: number;
  openAudits: number;
  lastAuditDate: string;
  issues: GovernanceIssue[];
}

export interface GovernanceIssue {
  id: string;
  type: string;
  severity: string;
  description: string;
  remediation: string;
}

export interface GovernancePolicy {
  id: string;
  name: string;
  type: string;
  rules: PolicyRule[];
  enforcement: string;
}

export interface PolicyRule {
  condition: string;
  action: string;
  exceptions: string[];
}

export interface GovernanceControl {
  type: string;
  implementation: string;
  validation: string;
  frequency: string;
}

export interface AuditConfig {
  enabled: boolean;
  events: string[];
  retention: string;
  storage: string;
}

export interface ComplianceConfig {
  frameworks: string[];
  requirements: ComplianceRequirement[];
  reporting: ComplianceReporting;
}

export interface ComplianceRequirement {
  id: string;
  framework: string;
  control: string;
  implementation: string;
}

export interface ComplianceReporting {
  frequency: string;
  format: string;
  recipients: string[];
}
