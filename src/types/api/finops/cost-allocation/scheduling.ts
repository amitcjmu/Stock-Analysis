/**
 * Scheduling and Automation Types
 *
 * Types for allocation scheduling, timing, dependencies, automation, and monitoring.
 * Covers execution schedules, failure handling, and rollback configuration.
 *
 * Generated by CC (Claude Code) - Modularized from cost-allocation.ts
 */

// Scheduling Types
export interface AllocationSchedule {
  frequency: 'real_time' | 'daily' | 'weekly' | 'monthly' | 'quarterly';
  timing: AllocationTiming;
  dependencies: ScheduleDependency[];
  automation: ScheduleAutomation;
  monitoring: ScheduleMonitoring;
}

export interface AllocationTiming {
  executionTime: string;
  timezone: string;
  businessDays: boolean;
  holidays: string[];
  cutoffTime: string;
}

export interface ScheduleDependency {
  type: 'data_availability' | 'approval' | 'external_system' | 'validation';
  source: string;
  timeout: string;
  action: 'wait' | 'proceed' | 'alert' | 'skip';
}

// Automation Configuration
export interface ScheduleAutomation {
  enabled: boolean;
  failureHandling: FailureHandling;
  notifications: ScheduleNotification[];
  rollback: RollbackConfig;
}

export interface FailureHandling {
  retries: number;
  interval: string;
  escalation: string[];
  fallback: string;
}

export interface ScheduleNotification {
  event: 'start' | 'success' | 'failure' | 'delay';
  recipients: string[];
  channels: string[];
  template: string;
}

export interface RollbackConfig {
  automated: boolean;
  triggers: string[];
  approval: boolean;
  timeline: string;
}

export interface ScheduleMonitoring {
  metrics: string[];
  alerts: string[];
  dashboard: string;
  reporting: string;
}
