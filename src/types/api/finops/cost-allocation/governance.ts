/**
 * Approval and Governance Types
 * 
 * Types for approval workflows, governance policies, compliance, and approval thresholds.
 * Covers allocation approvals, approval configurations, and governance structures.
 * 
 * Generated by CC (Claude Code) - Modularized from cost-allocation.ts
 */

import type { CostAmount } from '../cost-analysis';

// Approval Configuration Types
export interface AllocationApprovalConfig {
  required: string[];
  thresholds: AllocationApprovalThreshold[];
  workflow: string;
  timeout: string;
  escalation: string[];
}

export interface AllocationApprovalThreshold {
  type: 'amount' | 'percentage' | 'variance' | 'risk';
  value: number;
  approver: string;
  parallel: boolean;
}

// Approval Types
export interface AllocationApproval {
  id: string;
  type: 'execution' | 'adjustment' | 'exception' | 'override';
  status: 'pending' | 'approved' | 'rejected' | 'expired';
  approver: string;
  requestedAt: string;
  approvedAt?: string;
  rejectedAt?: string;
  amount?: CostAmount;
  justification: string;
  conditions: string[];
}