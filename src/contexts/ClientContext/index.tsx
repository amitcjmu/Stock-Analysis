// Re-export everything from the main ClientContext file
export { ClientProvider } from '../ClientContext';
export { useClient } from './hooks';
export type { Client, ClientContextType } from './types';