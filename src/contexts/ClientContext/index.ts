/**
 * ClientContext Index
 * Re-export hooks separately to avoid react-refresh warnings
 */

// eslint-disable-next-line react-refresh/only-export-components
export { useClient, withClient } from './hooks';
