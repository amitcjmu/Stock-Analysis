# Upstash Redis Production Environment Variables
# Copy these values from your Upstash Console after creating a Redis database

# ================================
# MANDATORY: Upstash Redis Configuration
# ================================

# Get these from Upstash Console → Your Database → Details
UPSTASH_REDIS_URL=https://your-endpoint.upstash.io
UPSTASH_REDIS_TOKEN=AxxCz...your-long-token-here

# ================================
# OPTIONAL: Redis Configuration
# ================================

# Enable Redis caching (default: true)
REDIS_ENABLED=true

# Default cache TTL in seconds (default: 3600 = 1 hour)
REDIS_DEFAULT_TTL=3600

# Upstash free tier daily command limit (default: 10000)
UPSTASH_DAILY_LIMIT=10000

# ================================
# Platform-Specific Instructions
# ================================

# RAILWAY BACKEND:
# 1. Go to Railway project → Variables tab
# 2. Add UPSTASH_REDIS_URL and UPSTASH_REDIS_TOKEN
# 3. Deploy service
# 4. Check logs for: "Connected to Upstash Redis (Production)"

# VERCEL FRONTEND:
# Usually NOT needed - frontend doesn't directly connect to Redis
# Only add if your frontend specifically needs Redis access

# ================================
# Verification
# ================================

# Test Redis connection:
# curl https://your-railway-app.com/api/v1/health | jq '.redis'

# Expected response:
# {
#   "redis": {
#     "status": "healthy",
#     "client_type": "upstash",
#     "latency_ms": 45.2
#   }
# }
