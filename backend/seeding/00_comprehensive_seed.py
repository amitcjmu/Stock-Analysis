#!/usr/bin/env python3
"""
Comprehensive Data Seeding Script - Entry Point
Generated by CC - delegates to modularized package

This script maintains backward compatibility with the original interface
while delegating to the modularized implementation in comprehensive_seed_modules/.

Usage:
    python seeding/00_comprehensive_seed.py [--cleanup] [--tenants=2]
"""

import asyncio
import sys
from pathlib import Path

# Add the parent directory to sys.path  # noqa: E402
sys.path.append(str(Path(__file__).parent.parent))

from seeding.comprehensive_seed_modules import (  # noqa: E402
    AGENT_PATTERNS,
    LLM_INTERACTIONS,
    TENANT_CONFIG,
    TEST_TENANT_1,
    TEST_TENANT_2,
    cleanup_existing_data,
    generate_dummy_embedding,
    main,
    seed_agent_data,
    seed_assets_and_dependencies,
    seed_comprehensive_data,
    seed_core_entities,
    seed_discovery_flows,
    seed_llm_usage,
)

# Re-export for backward compatibility
__all__ = [
    "TEST_TENANT_1",
    "TEST_TENANT_2",
    "TENANT_CONFIG",
    "AGENT_PATTERNS",
    "LLM_INTERACTIONS",
    "generate_dummy_embedding",
    "cleanup_existing_data",
    "seed_core_entities",
    "seed_discovery_flows",
    "seed_agent_data",
    "seed_llm_usage",
    "seed_assets_and_dependencies",
    "seed_comprehensive_data",
    "main",
]

if __name__ == "__main__":
    asyncio.run(main())
