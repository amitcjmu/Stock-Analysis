"""
Base Cache Key Patterns

Shared imports and base patterns for cache key generation modules.

Generated by CC (Claude Code)
"""

# CACHE_VERSION imported from parent module
from ..base import CACHE_VERSION  # noqa: F401


class BaseCacheKeys:
    """
    Base class containing shared utility methods and constants for cache key generation.

    Design Principles:
    - All keys include CACHE_VERSION for schema evolution
    - Multi-tenant isolation via client_account_id where applicable
    - Hierarchical structure for cascade invalidation
    - Consistent naming patterns for monitoring
    - Memory-efficient key lengths
    """

    @staticmethod
    def _build_key(*parts: str) -> str:
        """
        Build a cache key from parts, ensuring consistent format.

        Args:
            *parts: Cache key components to join

        Returns:
            Formatted cache key string
        """
        return ":".join(str(part) for part in parts if part)
